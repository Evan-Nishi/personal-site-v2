import React from 'react'
import styles from '../style/Left.module.css'

export default class leftBar extends React.Component{
    render(){
        return(
            <div id={styles.Left}/>
        )
    }
}